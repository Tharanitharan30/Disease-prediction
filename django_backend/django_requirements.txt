# ──────────────────────────────────────────────
#  Django Backend — requirements.txt
#  Install: pip install -r django_requirements.txt
# ──────────────────────────────────────────────

# Django core
django==5.0.4
djangorestframework==3.15.1    # REST API views + serializers
django-cors-headers==4.3.1     # CORS for React frontend

# HTTP client (proxy calls to FastAPI)
httpx==0.27.0

# Image field support
pillow==10.3.0
